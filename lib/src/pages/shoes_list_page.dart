import 'package:flutter/material.dart';

class ShoesListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Shoes Store app'),
      ),
    );
  }
}
